version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install -g @ionic/cli
        - npm ci
    build:
      commands:
        - ionic build --prod
  artifacts:
    baseDirectory: www
    files:
      - "**/*"
  cache:
    paths:
      - node_modules/**/*
